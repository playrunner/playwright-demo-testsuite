{
  "name": "playwright-demo-testsuite",
  "version": "1.0.0",
  "description": "Basic Playwright test suite demo",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^20.12.0",
    "typescript": "^5.4.0"
  }
}
